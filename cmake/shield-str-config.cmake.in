@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Shield-str depends on re2 and simdjson.
# When installed via cmake --install, these are expected to be available.
find_dependency(re2 QUIET)

include("${CMAKE_CURRENT_LIST_DIR}/shield-str-targets.cmake")

check_required_components(shield-str)
